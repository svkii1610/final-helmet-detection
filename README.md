# Safety Helmet Detection (Faster R-CNN, PyTorch)

รายงานและโค้ดประกอบโครงงานวิชา **01204466 Deep Learning (การเรียนรู้เชิงลึก)**
**"ระบบตรวจจับหมวกนิรภัยในเขตก่อสร้างด้วย Deep Learning"**

คณะวิศวกรรมศาสตร์ สาขาวิศวกรรมคอมพิวเตอร์ มหาวิทยาลัยเกษตรศาสตร์
ภาคต้น ปีการศึกษา 2568

---

## 👥 ผู้จัดทำ

- **นางสาวบุณยาพร บุญทับ** (6610505454)
  รับผิดชอบ: ออกแบบและพัฒนาโมเดล Deep Learning, Training, Evaluation, เขียนรายงานส่วน Model/Training/Eval

- **นางสาวไข่มุกข์ ธานะกิจ** (6610505292)
  รับผิดชอบ: เตรียมและจัดการข้อมูล, Implementation, สรุปผลการทดลอง, จัดรูปแบบรายงาน

**อาจารย์ผู้สอน:** อาจารย์ ภารุจ รัตนวรพันธุ์

---

## 📁 ไฟล์ในโปรเจกต์

| ไฟล์ | รายละเอียด |
|------|-----------|
| `README.md` | ไฟล์นี้ - คำแนะนำและข้อมูลโปรเจกต์ |
| `helmet_detection_project.ipynb` | Notebook สำหรับฝึกโมเดล, ประเมินผล และแสดงผลการทำนาย |
| `helmet_detect_final_report.pdf` | รายงานฉบับเต็ม (16 หน้า) - ทฤษฎี, สถาปัตยกรรม, ผลการทดลอง |

---

## 🎯 วัตถุประสงค์โครงงาน

1. แก้ไขปัญหาการไม่ปฏิบัติตามข้อกำหนดความปลอดภัยในพื้นที่ก่อสร้าง
2. พัฒนาระบบตรวจจับหมวกนิรภัยโดยอัตโนมัติด้วย AI และ Deep Learning
3. ลดข้อจำกัดของการตรวจสอบด้วยมนุษย์ (ความเมื่อยล้า, ความผิดพลาด, การตรวจไม่ต่อเนื่อง)
4. เพิ่มความปลอดภัยในสถานที่ทำงานและลดอุบัติเหตุ
5. สร้างโมเดล Deep Learning ที่ครบถ้วนตามหลักการพัฒนา AI
6. ประยุกต์ใช้เทคโนโลยี Deep Learning ในงานจริง (Real-world Application)
7. เป็นโครงงานตัวอย่างที่เหมาะสมสำหรับการเรียนรู้ระดับมหาวิทยาลัย

---

## 🧠 สรุปแนวทาง (Overview)

- **งาน:** Object Detection (ตรวจจับตำแหน่งและจำแนกประเภทวัตถุ)
- **โมเดล:** Faster R-CNN (ResNet-50 + FPN)
  - Backbone: ResNet-50 สำหรับดึงคุณลักษณะจากภาพ
  - FPN (Feature Pyramid Network): รวมคุณลักษณะหลายระดับความละเอียด
  - **Custom ROI Head**: MLP แบบกำหนดเอง
    `Linear → LayerNorm → SiLU → Dropout → Linear → SiLU`
    แยกเป็น Classifier Head และ BBox Regressor Head
- **Optimizer:** AdamW
  - Learning Rate: 7×10⁻⁴
  - Weight Decay: 1×10⁻²
- **เทคนิคเพิ่มประสิทธิภาพ:**
  - Mixed Precision Training (AMP) - เร่งความเร็วและลดการใช้หน่วยความจำ
  - ลดจำนวน Region Proposals และ ROI Sampling เพื่อความเร็ว
- **ขนาดภาพ:** 640×640 พิกเซล
- **จำนวน Epochs:** 12 epochs
- **ผลลัพธ์:** Precision ≈ 0.916, Recall ≈ 0.828

> รายละเอียดสถาปัตยกรรมและภาพแผนผัง: หัวข้อที่ 4 ใน `helmet_detect_final_report.pdf`

---

## 📚 ชุดข้อมูล (Dataset)

- **ชื่อ:** Construction Site Safety Image Dataset (Roboflow)
- **แหล่งที่มา:** [Kaggle Dataset](https://www.kaggle.com/datasets/snehilsanyal/construction-site-safety-image-dataset-roboflow)
- **จำนวนข้อมูล:** ประมาณ 5,000 ภาพ
- **รูปแบบ Annotation:** Pascal VOC (.xml)
  - 1 ภาพมี 1 ไฟล์ .xml คู่กัน (ชื่อไฟล์เหมือนกัน)
  - ภายในไฟล์ .xml ระบุชื่อคลาสและพิกัดของ bounding box
- **การแบ่งข้อมูล:**
  - Train: 80% - ใช้สำหรับฝึกโมเดล
  - Validation: 10% - ใช้สำหรับตรวจสอบระหว่างการฝึก
  - Test: 10% - ใช้สำหรับประเมินผลสุดท้าย
- **คลาส (Classes):**
  - `Helmet` - หมวกนิรภัย
  - `No Helmet / Head` - หัวคนที่ไม่สวมหมวกนิรภัย
  - `Person` - คน

**โครงสร้างโฟลเดอร์:**
```
Dataset/
├── train/
│   ├── images/
│   └── annotations/
├── val/
│   ├── images/
│   └── annotations/
└── test/
    ├── images/
    └── annotations/
```

---

## ⚙️ สิ่งแวดล้อมและการติดตั้ง (Environment)

- Python 3.10+
- PyTorch + torchvision (รองรับ CUDA ถ้ามี GPU)
- ไลบรารีพื้นฐาน: `numpy, matplotlib, tqdm, opencv-python`

> **แนะนำ:** รันทั้งหมดผ่าน Google Colab ได้ทันที (ตั้ง path ของ Dataset ให้ถูกก่อน Run All)

---

## 🚀 วิธีใช้งาน Notebook

### ขั้นตอนการรัน

1. **เปิด Notebook**
   - เปิดใน Jupyter Notebook: `jupyter notebook helmet_detection_project.ipynb`
   - หรือเปิดใน Google Colab (แนะนำ - มี GPU ให้ใช้ฟรี)

2. **ตั้งค่าพาธไปยัง Dataset**
   - แก้ไขพาธใน Notebook ให้ชี้ไปยังโฟลเดอร์ `Dataset/`
   - ตรวจสอบว่ามีโฟลเดอร์ `train/`, `val/`, `test/` พร้อม `images/` และ `annotations/`

3. **Run ตามลำดับเซลล์:**

   **ส่วนที่ 1: การเตรียมข้อมูล (Data Preparation)**
   - อ่านภาพและไฟล์ annotation (.xml) ด้วย `xml.etree.ElementTree`
   - แปลงข้อมูลเป็น list ของ bounding boxes (x_min, y_min, x_max, y_max) และ label id
   - ใช้ `torchvision.transforms` สำหรับ Resize((640, 640)) และ ToTensor()
   - สร้าง Custom Dataset class (`HelmetDataset`)
   - สร้าง DataLoader สำหรับ Train, Val, Test พร้อม `collate_fn`

   **ส่วนที่ 2: การสร้างโมเดล (Model Construction)**
   - โหลด Faster R-CNN (ResNet-50 + FPN) จาก PyTorch Vision Model Zoo
   - ปรับแต่ง Custom ROI Head แบบ Feedforward MLP
     - Linear Layer 1024 nodes → LayerNorm → SiLU → Dropout(0.3)
     - Linear Layer 1024 nodes → SiLU
   - แยกหัวเป็น Classifier Head และ BBox Regressor Head

   **ส่วนที่ 3: การฝึกสอน (Training)**
   - ตั้งค่า AdamW Optimizer (lr=7×10⁻⁴, weight_decay=1×10⁻²)
   - เปิดใช้งาน Automatic Mixed Precision (AMP)
   - ฝึกโมเดล 12 epochs
   - คำนวณ Loss รวมจาก 4 ส่วน: classifier, box_reg, objectness, rpn_box_reg
   - บันทึก Train Loss และ Validation Loss ทุก epoch
   - บันทึก Best Model (ที่มี validation loss ต่ำสุด)

   **ส่วนที่ 4: การประเมินผล (Evaluation)**
   - โหลด Best Model
   - ตั้งค่า threshold = 0.5 สำหรับคัดกรองผลทำนาย
   - คำนวณ Metrics: Precision, Recall, F1-Score, mAP
   - วาดกราฟ Loss Curve (Train vs Val Loss)

   **ส่วนที่ 5: การแสดงผล (Visualization)**
   - ทำนายภาพจากชุด Test
   - วาด Bounding Box พร้อม label และ confidence score
   - แสดงผลลัพธ์การตรวจจับหมวกนิรภัย

---

## 📈 การประเมินผล (Evaluation Metrics)

### ค่าชี้วัดที่ใช้

1. **Accuracy** - ความถูกต้องโดยรวม
2. **Precision (ความแม่นยำ)** - สัดส่วนของกรอบที่ทำนายถูกต้องจากทั้งหมดที่ทำนาย
3. **Recall (ความครอบคลุม)** - สัดส่วนของวัตถุจริงที่โมเดลสามารถตรวจจับได้
4. **F1-Score** - ค่ากลางระหว่าง Precision และ Recall
5. **mAP (mean Average Precision)** - ความแม่นยำเฉลี่ยในหลายระดับของค่า Threshold

### ผลลัพธ์

- **Precision:** ≈ 0.916 (91.6%)
- **Recall:** ≈ 0.828 (82.8%)
- **Threshold:** 0.5 (ปรับได้ตามเป้าหมาย: ลด false alarm หรือเพิ่มการจับให้ครบ)
- กราฟ Loss แสดงแนวโน้มลดลงอย่างต่อเนื่อง ไม่เกิด overfitting

> ดูกราฟ Loss Curve และตัวอย่างภาพผลการทำนายใน Section 7.2 และ 7.4 ของ `helmet_detect_final_report.pdf`

---

## 🧩 จุดเด่นเชิงเทคนิค

1. **Custom ROI Head แบบ MLP**
   - แทน FastRCNNPredictor มาตรฐาน
   - ใช้ Linear Layer + LayerNorm + SiLU Activation + Dropout
   - เพิ่มเสถียรภาพการเรียนรู้และลด overfitting เมื่อข้อมูลจำกัด

2. **Two-Stage Detector Architecture**
   - Region Proposal Network (RPN): เสนอกรอบวัตถุที่น่าจะมีของ
   - ROI Head: จำแนกคลาสและปรับกรอบให้แม่นยำ
   - ให้ความแม่นยำสูงกว่า One-stage detectors เช่น YOLO

3. **Automatic Mixed Precision (AMP)**
   - เร่งความเร็วการฝึกโมเดล
   - ลดการใช้หน่วยความจำบน GPU
   - ไม่สูญเสียความแม่นยำของผลลัพธ์

4. **Optimization สำหรับความเร็ว**
   - ลดจำนวน Region Proposals
   - ปรับ `roi_heads.batch_size_per_image` จาก 128 เหลือ 64
   - เทรนและทำนายเร็วขึ้นโดยไม่กระทบความแม่นยำ

5. **Transfer Learning**
   - ใช้ ResNet-50 ที่ pretrain แล้วบน ImageNet
   - Fine-tune บนชุดข้อมูลหมวกนิรภัย
   - ใช้ข้อมูลน้อยแต่ได้ผลลัพธ์ดี

---

## 🖼️ ตัวอย่างผลลัพธ์

ดูภาพตัวอย่างพร้อมกรอบทำนายและกราฟ Loss ใน:
- **Section 7.2** (Loss Curve) - แสดงกราฟ Train vs Validation Loss
- **Section 7.4** (ตัวอย่างผลการทำนาย) - ภาพตรวจจับในสภาพแวดล้อมจริง
- ส่วน **Visualization** ตอนท้ายของ `helmet_detection_project.ipynb`

โมเดลสามารถตรวจจับหมวกนิรภัยได้ดีทั้งในสภาพแสงที่ซับซ้อน, มุมมองเฉียง และพื้นหลังที่มีรายละเอียดสูง

---

## 🔗 เอกสารอ้างอิงและงานที่เกี่ยวข้อง

### งานวิจัยหลัก
- **Faster R-CNN:** Ren et al., 2015 - สถาปัตยกรรม Two-stage detector
- **FPN (Feature Pyramid Network):** Lin et al., 2017 - รวมคุณลักษณะหลายระดับ
- **ResNet:** He et al., 2016 - Backbone network สำหรับดึงคุณลักษณะ

### งานที่เกี่ยวข้องกับหมวกนิรภัย
- **YOLOv5** (IEEE, 2021): เน้นประสิทธิภาพ Real-time แต่ความแม่นยำต่ำกว่าในสภาพแสงซับซ้อน
- **Faster R-CNN** (Elsevier, 2020): ให้ความแม่นยำสูงและเหมาะกับการตรวจจับในสภาพแวดล้อมจริง

> สรุปเปรียบเทียบงานที่เกี่ยวข้องอยู่ในหัวข้อที่ 8 ของ `helmet_detect_final_report.pdf`

---

## 💡 เหตุผลที่ใช้ Deep Learning (Faster R-CNN)

1. **ปัญหาเป็น Object Detection** - ต้องแยกทั้งตำแหน่งและประเภทของวัตถุ ซับซ้อนเกินกว่าจะใช้การประมวลผลภาพแบบดั้งเดิม

2. **เรียนรู้คุณลักษณะอัตโนมัติ** - Deep Learning สามารถเรียนรู้ฟีเจอร์จากภาพได้เอง ไม่ต้องเขียนกฎ (feature engineering) ด้วยตนเอง

3. **ความแม่นยำสูง** - Faster R-CNN มีสถาปัตยกรรม Two-stage (RPN + ROI Head) ให้ความแม่นยำสูงกว่า One-stage detectors

4. **Transfer Learning** - สามารถ fine-tune จากโมเดลที่ pretrain แล้ว ทำให้ใช้ dataset จำนวนน้อยแต่ยังได้ผลลัพธ์ดี

5. **รองรับการขยายงาน** - สามารถเพิ่มคลาสใหม่ (เช่น vest, คนไม่สวมหมวก) หรือขยายไปยังสถานการณ์อื่นได้ง่าย

---

## 🚧 ข้อจำกัดและงานต่อยอด

### ข้อจำกัด
- ภาพที่แสงน้อยหรือหมวกสีใกล้เคียงฉากหลังอาจตรวจพลาด
- ความเร็วในการประมวลผลยังไม่เหมาะสำหรับ Real-time monitoring

### งานต่อยอด
1. **Real-time Detection** - ทำ real-time detection ด้วย YOLOv5 หรือ YOLOv8
2. **เพิ่มคลาส** - เพิ่มการตรวจจับ "vest" (เสื้อสะท้อนแสง)
3. **ระบบแจ้งเตือน** - สร้าง Dashboard สำหรับระบบแจ้งเตือนอัตโนมัติ
4. **ปรับปรุงข้อมูล** - เพิ่มข้อมูลในสภาพแสงน้อยและมุมมองที่หลากหลาย
5. **Edge Deployment** - Deploy โมเดลบน Edge devices เช่น Raspberry Pi หรือ Jetson Nano

---

## 🔗 ลิงก์โค้ดและทรัพยากร

- **GitHub Repository:** https://github.com/svkii1610/final-helmet-detection.git
- **Dataset:** [Construction Site Safety Image Dataset (Kaggle)](https://www.kaggle.com/datasets/snehilsanyal/construction-site-safety-image-dataset-roboflow)

---

## 📜 License

โครงงานนี้จัดทำเพื่อการศึกษาและวิจัยภายในรายวิชา 01204466 เท่านั้น

> หากต้องการเผยแพร่สาธารณะ โปรดเพิ่มไฟล์ `LICENSE` ให้ชัดเจน (เช่น MIT/Apache-2.0) และตรวจสอบสิทธิ์ของ Dataset ที่ใช้งาน

---

**© 2568 คณะวิศวกรรมศาสตร์ มหาวิทยาลัยเกษตรศาสตร์**